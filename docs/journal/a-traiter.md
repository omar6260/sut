# À traiter — problèmes repérés hors du périmètre de la phase en cours

Règle : on note ici, on ne corrige pas. Une ligne par point, la phase qui devrait s'en charger, et où on l'a vu.

| Date | Repéré en | Problème | Où | Phase proposée |
|---|---|---|---|---|
| 2026-09-16 | Phase 00 | `docs/source/` est vide : le cahier des charges et le guide d'utilisation (.docx) n'ont pas encore été déposés. Les sources de vérité n° 1 et 2 manquent aux agents. | `docs/source/README.md` | Avant phase 01 (Oumar) |
| 2026-09-16 | Phase 00 | `npm run emulators` et `npm run test:rules` supposent un `firebase.json` et un `.firebaserc` qui n'existent pas encore. | `package.json` | Phase 04 |
| 2026-09-16 | Phase 00 | `npm --prefix functions test` (revue de phase) échoue tant que `functions/package.json` n'existe pas. | `functions/` | Phase 06 |
| 2026-09-16 | Phase 00 | Le prototype charge des scripts tiers non épinglés (Jitsi `meet.jit.si`, FFmpeg.wasm via unpkg, Google Identity Services). Les tests de caractérisation devront les bloquer ou les simuler (`page.route`) pour rester rapides et déterministes. | `legacy/suktum-app.html` l. 1–43, 234–235 | Phase 01 (tests) puis 03 (C6/C7) |
| 2026-09-16 | Phase 01 | Écran caméra (`#screen-camera-publish`) : la ligne « PUBLIER / CRÉER » est recouverte par la barre d'onglets `#tabbar` sur un viewport Pixel 7 (Playwright : « `<nav id="tabbar">` intercepts pointer events »). Le lien « CRÉER » est intouchable ; seule la vignette galerie mène à l'import. | `legacy/suktum-app.html` l. 1896–1897 | Phase 03 (CSS) — décision produit à confirmer |
| 2026-09-16 | Phase 01 | Le pays « Sénégal » bascule automatiquement l'interface en wolof (`getLikelyLanguageForCountry`), sauf choix manuel. Le guide d'utilisation est en français : à confirmer avec Gorgui que c'est voulu. | `legacy/suktum-app.html` `getLikelyLanguageForCountry` | Décision produit |
| 2026-09-16 | Phase 01 | Fil : la colonne d'actions d'une publication (12 boutons : aimer, ne pas aimer, réaction, commenter, enregistrer, envoyer, WhatsApp, ami, pas intéressé, repartager, citer, signaler) déborde au-dessus du viewport Pixel 7 ; le bouton « aimer » est recouvert par la cloche `#global-notif-btn` et le bouton boutique. Intouchable au doigt. | `legacy/suktum-app.html` rendu `.feed-actions` ~l. 13417 | Phase 03 (CSS) — décision produit à confirmer |
| 2026-09-16 | Phase 02 | `coinbalance:` est écrit en stockage **privé** (`shared=false`, l. 7236, 34655, 34663, 35468) : le solde de pièces est propre à l'appareil, pas au compte. Un utilisateur qui change de téléphone perd (ou duplique) ses pièces ; l'ajustement admin l. 35213 écrit dans l'espace privé de l'admin, pas celui de l'utilisateur — probablement sans effet. | `legacy/suktum-app.html` | Phase 06 (transaction serveur) — à confirmer avec Gorgui que ce n'est pas voulu |
| 2026-09-16 | Phase 02 | Tableau financier : `isWithinFinancePeriod(p.createdAt)` filtre `coinpurchase:` (l. 35047) et `edupurchase:` (l. 35041) alors que ces documents portent `purchasedAt`, pas `createdAt` → filtre de période probablement inopérant. | `renderFinanceDashboard` | Phase 06 (agrégats serveur) |
| 2026-09-16 | Phase 02 | Deux jeux de clés Google Cloud pour le même service : `settings:google_vision_api_key`/`google_video_api_key` (l. 9535-9542) et `settings:gcvVisionKey`/`gcvVideoKey` (l. 26288-26299), écrits par deux écrans admin différents. | `docs/inventaire/appels-externes.md` | Phase 06 (un seul secret) |
| 2026-09-16 | Phase 02 | Les 8 appels `api.anthropic.com` n'envoient aucune clé (l. 15697, 19115, 26559, 26603, 31289, 31443, 31494, 31516) : inopérants hors de l'environnement Claude. Modèle demandé `claude-sonnet-4-6`. | `appels-externes.md` § 1 | Phase 06 |
| 2026-09-16 | Phase 02 | `GOOGLE_CLIENT_ID = ""` (l. 6983) : la connexion Google du prototype n'a jamais pu fonctionner. | l. 6983 | Phase 05 (Firebase Auth) |
| 2026-09-16 | Phase 02 | `respondToNegotiation` (l. 26798) : aucune garde sur `currentUser` — l'acheteur peut accepter sa propre offre, qui devient le prix de commande (l. 27198). | l. 26798 | Phase 06 |
| 2026-09-16 | Phase 02 | `deleteMyOwnAccount` (l. 7983) lit `sellerinternalnote:<soi>` (note interne admin) et la recopie dans l'archive : fuite d'une note confidentielle vers le client. | l. 7976-7997 | Phase 06 |
| 2026-09-16 | Phase 02 | `quiz.correctIndex` (l. 18574) et `fullexam.questions[].correctIndex` (l. 18378) sont stockés dans le document lu par les élèves ; `quizsubmission.correct` (l. 18629) est calculé par l'élève. | Éducation | Phase 06 (correction serveur) |
| 2026-09-16 | Phase 02 | Intégration Yango inachevée : coordonnées `[0, 0]` en dur et « Adresse du vendeur (à compléter) » (l. 26489-26490). | `requestYangoDelivery` | Phase 10 |
| 2026-09-16 | Phase 02 | Les sous-agents `.claude/agents/*` (cartographe, architecte…) ne sont chargés qu'au démarrage d'une session : ils étaient absents de cette session (kit déplacé à la racine en cours de route). Phase 02 exécutée avec des agents génériques portant le rôle du cartographe en prompt. Démarrer la phase 03 dans une session neuve. | `.claude/agents/` | Immédiat (procédure) |
| 2026-09-16 | Phase 02 (architecte) | **Prérequis phase 04** — règles Firestore ≠ filtres : ~60 % des `safeList` sont globaux (`dm:` l. 27693, `enrollment:` 18 sites, `order:` l. 27297, `notif:` l. 11508). Avec des règles restrictives ces écrans seront vides. Solution retenue à concevoir : collections plates `kv_<prefix>` + champ `readers[]` + `array-contains` dans l'adaptateur. | `docs/inventaire/relecture-architecte.md` § 3 | Phase 04 |
| 2026-09-16 | Phase 02 (architecte) | **Prérequis phase 04** — limite Firestore 1 Mio/document : 24 préfixes portent des médias base64 (`post.data` 3,5 Mo, `dm` 2 Mo/message, `coursechat` 10 Mo). La première vidéo publiée échouera : prévoir un déport de blob > 900 Kio vers Storage dans l'adaptateur, ou inverser 04/07. | `prefixes.md` colonne « Médias base64 » | Phase 04 (décision D9) |
| 2026-09-16 | Phase 02 (architecte) | Opt-out de notifications (`livenotifypref`, `postnotifypref`, prefs de chat) : lu en privé pour autrui → `null` → jamais respecté dans le prototype. L'adaptateur doit renvoyer `null` (pas d'exception) sur `permission-denied` pour ne pas changer le comportement avant la Function de fan-out. | l. 13761, 13780, 18780, 18813 | Phase 04 (comportement), 06 (fan-out) |
| 2026-09-16 | Phase 02 (architecte) | `notif:<id>` non indexé par destinataire : chaque badge/ouverture du fil liste toutes les notifications de la plateforme (l. 11508). | l. 11452-11508 | Phase 08 (chemin chaud n° 2) |
