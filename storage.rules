rules_version = '2';
// Phase 04 : Storage non utilisé par le client (médias en phase 07). Tout est refusé.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} { allow read, write: if false; }
  }
}
